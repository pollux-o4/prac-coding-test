# 코딩테스트 환경 설정 가이드라인

## 🎯 환경 설정의 중요성

효율적인 코딩테스트 준비를 위해서는 적절한 개발 환경이 필수입니다. 좋은 환경 설정은 문제 풀이 속도를 크게 향상시킬 수 있습니다.

## 💻 추천 개발 환경

### 1. Python 환경 설정

#### Python 설치
1. **Python 공식 사이트에서 다운로드**
   - https://www.python.org/downloads/
   - 최신 버전 (3.8 이상) 설치
   - 설치 시 "Add Python to PATH" 체크

2. **설치 확인**
   ```bash
   python --version
   pip --version
   ```

#### 가상환경 설정 (선택사항)
```bash
# 가상환경 생성
python -m venv coding_test_env

# 가상환경 활성화 (Windows)
coding_test_env\Scripts\activate

# 가상환경 활성화 (Mac/Linux)
source coding_test_env/bin/activate
```

#### 필수 라이브러리 설치
```bash
pip install numpy
pip install pandas
pip install matplotlib
pip install jupyter
```

### 2. IDE/에디터 선택

#### VS Code (추천 ⭐⭐⭐⭐⭐)
**설치 및 설정:**
1. https://code.visualstudio.com/ 에서 다운로드
2. Python 확장 프로그램 설치
3. 설정 파일 생성

**VS Code 설정 파일 (.vscode/settings.json):**
```json
{
    "python.defaultInterpreterPath": "python",
    "python.formatting.provider": "black",
    "editor.formatOnSave": true,
    "editor.rulers": [80],
    "files.autoSave": "onFocusChange",
    "python.linting.enabled": true,
    "python.linting.pylintEnabled": true
}
```

#### PyCharm (추천 ⭐⭐⭐⭐)
**장점:**
- 강력한 디버깅 기능
- 자동 완성 기능 우수
- 프로젝트 관리 기능

**단점:**
- 무거운 프로그램
- 유료 버전이 있음

#### Jupyter Notebook (추천 ⭐⭐⭐)
**장점:**
- 인터랙티브한 개발
- 결과를 바로 확인 가능
- 마크다운과 코드 혼용

**단점:**
- 코딩테스트 환경과 다름
- 파일 크기가 큼

### 3. 온라인 IDE (백업용)

#### Replit
- https://replit.com/
- 무료로 사용 가능
- 다양한 언어 지원

#### Google Colab
- https://colab.research.google.com/
- Python 전용
- GPU 사용 가능

## 🔧 코딩테스트 전용 설정

### 1. 빠른 입출력 템플릿
**template.py 파일 생성:**
```python
import sys
input = sys.stdin.readline

def solve():
    # 여기에 문제 해결 코드 작성
    pass

if __name__ == "__main__":
    solve()
```

### 2. 자주 사용하는 함수들
**utils.py 파일 생성:**
```python
import sys
from collections import defaultdict, Counter, deque
from itertools import permutations, combinations, product
import heapq
import math
import bisect

# 빠른 입력
def fast_input():
    return sys.stdin.readline().rstrip()

# 2차원 배열 생성
def create_2d_array(n, m, default=0):
    return [[default] * m for _ in range(n)]

# 배열 출력 (디버깅용)
def print_array(arr):
    for row in arr:
        print(*row)

# 조합 생성
def get_combinations(arr, r):
    return list(combinations(arr, r))

# 순열 생성
def get_permutations(arr, r):
    return list(permutations(arr, r))
```

### 3. 디버깅 도구 설정
**debug.py 파일 생성:**
```python
import sys
from io import StringIO

def debug_print(*args, **kwargs):
    print(*args, **kwargs, file=sys.stderr)

def capture_output(func, *args, **kwargs):
    """함수의 출력을 캡처하는 함수"""
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        result = func(*args, **kwargs)
        output = sys.stdout.getvalue()
        return result, output
    finally:
        sys.stdout = old_stdout
```

## 📁 프로젝트 구조 설정

### 1. 폴더 구조
```
CO_TE/
├── templates/
│   ├── template.py
│   ├── utils.py
│   └── debug.py
├── problems/
│   ├── solved/
│   └── unsolved/
├── algorithms/
│   ├── sorting/
│   ├── searching/
│   └── dp/
└── notes/
    ├── concepts.md
    └── tips.md
```

### 2. 문제 풀이 파일 명명 규칙
- **백준**: `boj_문제번호.py`
- **프로그래머스**: `pg_문제명.py`
- **리트코드**: `lc_문제명.py`

## ⚙️ VS Code 확장 프로그램

### 필수 확장 프로그램
1. **Python** - Microsoft
2. **Python Indent** - Kevin Rose
3. **Python Docstring Generator** - Nils Werner
4. **Python Test Explorer** - Little Fox Team
5. **Python Type Hint** - nhoizey
6. **autoDocstring** - njpwerner

### 유용한 확장 프로그램
1. **GitLens** - Git 기능 강화
2. **Bracket Pair Colorizer** - 괄호 색상 구분
3. **Indent Rainbow** - 들여쓰기 색상 구분
4. **Path Intellisense** - 경로 자동 완성

## 🎯 코딩테스트 환경 최적화

### 1. 단축키 설정
**VS Code 키보드 단축키:**
- `Ctrl + /`: 주석 처리
- `Alt + Shift + F`: 코드 포맷팅
- `F5`: 디버깅 시작
- `Ctrl + F5`: 디버깅 없이 실행

### 2. 스니펫 설정
**VS Code 스니펫 (Python):**
```json
{
    "Print to console": {
        "prefix": "print",
        "body": [
            "print($1)"
        ],
        "description": "Print to console"
    },
    "Fast input": {
        "prefix": "fastinput",
        "body": [
            "import sys",
            "input = sys.stdin.readline",
            "",
            "$1"
        ],
        "description": "Fast input setup"
    }
}
```

### 3. 테스트 환경 설정
**test_template.py:**
```python
def test_solution():
    # 테스트 케이스들
    test_cases = [
        {
            "input": [1, 2, 3],
            "expected": 6
        },
        {
            "input": [4, 5, 6],
            "expected": 15
        }
    ]
    
    for i, test_case in enumerate(test_cases):
        result = solve(test_case["input"])
        if result == test_case["expected"]:
            print(f"테스트 {i+1}: 통과")
        else:
            print(f"테스트 {i+1}: 실패 (예상: {test_case['expected']}, 실제: {result})")

def solve(arr):
    # 여기에 문제 해결 코드 작성
    return sum(arr)

if __name__ == "__main__":
    test_solution()
```

## 📊 성능 모니터링

### 1. 실행 시간 측정
```python
import time

def measure_time(func):
    def wrapper(*args, **kwargs):
        start_time = time.time()
        result = func(*args, **kwargs)
        end_time = time.time()
        print(f"실행 시간: {end_time - start_time:.4f}초")
        return result
    return wrapper

@measure_time
def my_function():
    # 함수 내용
    pass
```

### 2. 메모리 사용량 측정
```python
import psutil
import os

def get_memory_usage():
    process = psutil.Process(os.getpid())
    return process.memory_info().rss / 1024 / 1024  # MB

print(f"메모리 사용량: {get_memory_usage():.2f} MB")
```

## 🚀 온라인 코딩테스트 준비

### 1. 주요 플랫폼
- **백준**: https://www.acmicpc.net/
- **프로그래머스**: https://programmers.co.kr/
- **리트코드**: https://leetcode.com/
- **코드포스**: https://codeforces.com/

### 2. 플랫폼별 환경 설정
**백준:**
- Python 3 사용
- 빠른 입출력 필수

**프로그래머스:**
- 함수 형태로 제출
- 클래스 형태 문제 주의

**리트코드:**
- 클래스 메서드 형태
- 테스트 케이스 제공

## 📝 체크리스트

### 기본 설정
- [ ] Python 설치 및 PATH 설정
- [ ] VS Code 설치 및 Python 확장 프로그램 설치
- [ ] 가상환경 설정 (선택사항)
- [ ] 필수 라이브러리 설치

### 템플릿 설정
- [ ] 빠른 입출력 템플릿 생성
- [ ] 유틸리티 함수 파일 생성
- [ ] 디버깅 도구 설정
- [ ] 스니펫 설정

### 환경 최적화
- [ ] 단축키 숙지
- [ ] 테스트 환경 구축
- [ ] 성능 측정 도구 설정
- [ ] 온라인 플랫폼 계정 생성

## 🎯 다음 단계

환경 설정이 완료되었다면 다음 단계로 진행하세요:

1. **기본 문법 연습**: 선택한 언어의 기본 문법 숙지
2. **자료구조 학습**: 배열, 스택, 큐 등 기본 자료구조 이해
3. **알고리즘 기초**: 시간복잡도, 공간복잡도 개념 학습
4. **문제 풀이 시작**: 기초 문제부터 차근차근 풀어보기

---

**💡 팁**
- 환경 설정은 한 번만 하면 되므로 꼼꼼하게 설정하세요
- 자주 사용하는 템플릿은 외워두세요
- 디버깅 도구를 활용하여 효율적으로 문제를 해결하세요
- 온라인 플랫폼의 환경에 익숙해지세요 