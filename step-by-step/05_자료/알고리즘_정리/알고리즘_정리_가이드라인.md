# 알고리즘 정리 가이드라인

## 🎯 알고리즘 정리의 중요성

알고리즘 정리는 학습한 내용을 체계적으로 정리하고, 나중에 빠르게 참조할 수 있도록 하는 중요한 학습 방법입니다. 개인만의 노트를 만들어 효율적인 복습과 실전 활용을 도와줍니다.

## 📊 알고리즘 정리 방법

### 1. 체계적 분류
**분류 기준:**
- **유형별**: 그리디, DP, 그래프, 문자열 등
- **난이도별**: 기본, 중급, 고급
- **기업별**: 카카오, 네이버, 삼성 등
- **주제별**: 정렬, 탐색, 최적화 등

**정리 구조:**
```python
def algorithm_organization():
    structure = {
        "basic_algorithms": {
            "sorting": ["버블", "선택", "삽입", "병합", "퀵", "힙"],
            "searching": ["선형", "이진", "해시"],
            "data_structures": ["배열", "리스트", "스택", "큐", "힙", "트리"]
        },
        "intermediate_algorithms": {
            "greedy": ["동전 거스름돈", "활동 선택", "배낭 문제"],
            "dp": ["피보나치", "LIS", "배낭 문제", "편집 거리"],
            "graph": ["DFS", "BFS", "다익스트라", "플로이드-워셜"]
        },
        "advanced_algorithms": {
            "string": ["KMP", "라빈-카프", "허프만 코딩"],
            "math": ["소수", "GCD", "LCM", "모듈로 연산"],
            "optimization": ["이진 탐색", "파라메트릭 서치", "슬라이딩 윈도우"]
        }
    }
    return structure
```

### 2. 코드 템플릿 정리
```python
def code_templates():
    templates = {
        "binary_search": """
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    
    while left <= right:
        mid = (left + right) // 2
        
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    
    return -1
        """,
        
        "dfs": """
def dfs(graph, start, visited=None):
    if visited is None:
        visited = set()
    
    visited.add(start)
    print(start, end=' ')
    
    for neighbor in graph[start]:
        if neighbor not in visited:
            dfs(graph, neighbor, visited)
        """,
        
        "dp_fibonacci": """
def fibonacci_dp(n):
    if n <= 1:
        return n
    
    dp = [0] * (n + 1)
    dp[1] = 1
    
    for i in range(2, n + 1):
        dp[i] = dp[i-1] + dp[i-2]
    
    return dp[n]
        """
    }
    return templates
```

### 3. 문제 해결 패턴 정리
```python
def problem_solving_patterns():
    patterns = {
        "greedy": {
            "pattern": "각 단계에서 최적 선택",
            "examples": ["동전 거스름돈", "활동 선택", "배낭 문제"],
            "key_points": ["정당성 증명", "정렬 활용", "탐욕적 선택"]
        },
        "dp": {
            "pattern": "부분 문제의 해를 저장하여 재사용",
            "examples": ["피보나치", "LIS", "편집 거리"],
            "key_points": ["점화식", "메모이제이션", "타뷸레이션"]
        },
        "graph": {
            "pattern": "정점과 간선으로 구성된 구조 탐색",
            "examples": ["DFS", "BFS", "최단 경로"],
            "key_points": ["방문 체크", "인접 리스트/행렬", "사이클 감지"]
        }
    }
    return patterns
```

## 🔧 알고리즘별 상세 정리

### 1. 정렬 알고리즘
```python
# 버블 정렬
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

# 퀵 정렬
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    return quick_sort(left) + middle + quick_sort(right)

# 병합 정렬
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    
    return merge(left, right)

def merge(left, right):
    result = []
    i = j = 0
    
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i])
            i += 1
        else:
            result.append(right[j])
            j += 1
    
    result.extend(left[i:])
    result.extend(right[j:])
    return result
```

### 2. 탐색 알고리즘
```python
# 이진 탐색
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    
    while left <= right:
        mid = (left + right) // 2
        
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    
    return -1

# Lower Bound
def lower_bound(arr, target):
    left, right = 0, len(arr)
    
    while left < right:
        mid = (left + right) // 2
        
        if arr[mid] < target:
            left = mid + 1
        else:
            right = mid
    
    return left

# Upper Bound
def upper_bound(arr, target):
    left, right = 0, len(arr)
    
    while left < right:
        mid = (left + right) // 2
        
        if arr[mid] <= target:
            left = mid + 1
        else:
            right = mid
    
    return left
```

### 3. 그래프 알고리즘
```python
from collections import deque

# DFS
def dfs(graph, start, visited=None):
    if visited is None:
        visited = set()
    
    visited.add(start)
    print(start, end=' ')
    
    for neighbor in graph[start]:
        if neighbor not in visited:
            dfs(graph, neighbor, visited)

# BFS
def bfs(graph, start):
    visited = set()
    queue = deque([start])
    visited.add(start)
    
    while queue:
        vertex = queue.popleft()
        print(vertex, end=' ')
        
        for neighbor in graph[vertex]:
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append(neighbor)

# 다익스트라
import heapq

def dijkstra(graph, start):
    n = len(graph)
    distances = [float('inf')] * n
    distances[start] = 0
    
    pq = [(0, start)]
    
    while pq:
        dist, node = heapq.heappop(pq)
        
        if dist > distances[node]:
            continue
        
        for neighbor, weight in graph[node]:
            new_dist = dist + weight
            if new_dist < distances[neighbor]:
                distances[neighbor] = new_dist
                heapq.heappush(pq, (new_dist, neighbor))
    
    return distances
```

### 4. 동적 프로그래밍
```python
# 피보나치 (메모이제이션)
def fibonacci_memo(n, memo=None):
    if memo is None:
        memo = {}
    
    if n in memo:
        return memo[n]
    
    if n <= 1:
        return n
    
    memo[n] = fibonacci_memo(n-1, memo) + fibonacci_memo(n-2, memo)
    return memo[n]

# 피보나치 (타뷸레이션)
def fibonacci_tab(n):
    if n <= 1:
        return n
    
    dp = [0] * (n + 1)
    dp[1] = 1
    
    for i in range(2, n + 1):
        dp[i] = dp[i-1] + dp[i-2]
    
    return dp[n]

# LIS (최장 증가 부분 수열)
def longest_increasing_subsequence(arr):
    if not arr:
        return 0
    
    n = len(arr)
    dp = [1] * n
    
    for i in range(1, n):
        for j in range(i):
            if arr[i] > arr[j]:
                dp[i] = max(dp[i], dp[j] + 1)
    
    return max(dp)
```

### 5. 그리디 알고리즘
```python
# 동전 거스름돈
def coin_change_greedy(amount, coins):
    coins.sort(reverse=True)
    count = 0
    
    for coin in coins:
        if amount >= coin:
            count += amount // coin
            amount %= coin
    
    return count if amount == 0 else -1

# 활동 선택
def activity_selection(activities):
    activities.sort(key=lambda x: x[1])
    
    selected = []
    last_end = 0
    
    for start, end in activities:
        if start >= last_end:
            selected.append((start, end))
            last_end = end
    
    return selected

# 배낭 문제 (분할 가능)
def fractional_knapsack(weights, values, capacity):
    items = [(values[i], weights[i], values[i]/weights[i]) 
             for i in range(len(weights))]
    items.sort(key=lambda x: x[2], reverse=True)
    
    total_value = 0
    remaining_capacity = capacity
    
    for value, weight, ratio in items:
        if remaining_capacity >= weight:
            total_value += value
            remaining_capacity -= weight
        else:
            total_value += ratio * remaining_capacity
            break
    
    return total_value
```

## 📝 알고리즘 정리 템플릿

### 1. 알고리즘 카드 템플릿
```python
def algorithm_card_template():
    template = {
        "name": "알고리즘 이름",
        "category": "분류 (그리디, DP, 그래프 등)",
        "time_complexity": "시간복잡도",
        "space_complexity": "공간복잡도",
        "description": "알고리즘 설명",
        "use_cases": ["사용 사례 1", "사용 사례 2"],
        "code": "구현 코드",
        "key_points": ["핵심 포인트 1", "핵심 포인트 2"],
        "related_problems": ["관련 문제 1", "관련 문제 2"]
    }
    return template
```

### 2. 문제 해결 노트 템플릿
```python
def problem_solving_note_template():
    template = {
        "problem_id": "문제 번호",
        "problem_name": "문제 이름",
        "difficulty": "난이도",
        "category": "카테고리",
        "approach": "접근 방법",
        "algorithm": "사용한 알고리즘",
        "time_complexity": "시간복잡도",
        "space_complexity": "공간복잡도",
        "code": "해결 코드",
        "test_cases": ["테스트 케이스 1", "테스트 케이스 2"],
        "notes": "추가 노트",
        "related_problems": ["관련 문제 1", "관련 문제 2"]
    }
    return template
```

### 3. 기업별 문제 정리 템플릿
```python
def company_problem_template():
    template = {
        "company": "기업명",
        "year": "연도",
        "problem_type": "문제 유형",
        "difficulty": "난이도",
        "approach": "해결 접근법",
        "key_insights": ["핵심 인사이트 1", "핵심 인사이트 2"],
        "code": "해결 코드",
        "similar_problems": ["유사한 문제 1", "유사한 문제 2"],
        "tips": ["팁 1", "팁 2"]
    }
    return template
```

## 🚀 효율적인 정리 방법

### 1. 디지털 노트 활용
```python
def digital_note_strategy():
    tools = {
        "notion": "체계적 정리, 템플릿 활용",
        "obsidian": "연결된 노트, 마크다운",
        "github": "코드 버전 관리, 협업",
        "anki": "간격 반복, 암기"
    }
    return tools
```

### 2. 시각적 정리
```python
def visual_organization():
    methods = {
        "mind_map": "알고리즘 간 관계 정리",
        "flowchart": "알고리즘 흐름도",
        "comparison_table": "알고리즘 비교표",
        "timeline": "학습 진도 타임라인"
    }
    return methods
```

### 3. 정기적 복습
```python
def review_schedule():
    schedule = {
        "daily": "오늘 학습한 내용 복습",
        "weekly": "주간 학습 내용 정리",
        "monthly": "월간 성과 분석 및 계획 수정"
    }
    return schedule
```

## 🎯 알고리즘 정리 체크리스트

### 기본 정리 체크리스트
- [ ] 알고리즘 분류 체계 수립
- [ ] 코드 템플릿 정리
- [ ] 시간/공간 복잡도 정리
- [ ] 사용 사례 정리
- [ ] 관련 문제 링크 정리

### 고급 정리 체크리스트
- [ ] 알고리즘 증명 정리
- [ ] 최적화 기법 정리
- [ ] 예외 상황 정리
- [ ] 디버깅 팁 정리
- [ ] 실전 활용 팁 정리

### 지속적 관리 체크리스트
- [ ] 정기적 업데이트
- [ ] 새로운 문제 추가
- [ ] 개선점 반영
- [ ] 검색 기능 구현
- [ ] 백업 및 동기화

## 🚀 다음 단계

알고리즘 정리를 마스터했다면 다음 단계로 진행하세요:

1. **문제 해설**: 정리한 내용을 바탕으로 문제 해설 작성
2. **팁 노트**: 실전에서 활용할 수 있는 팁 정리
3. **성과 분석**: 정리한 내용을 바탕으로 학습 성과 분석

---

**💡 팁**
- 개인만의 체계적인 분류 체계를 만드세요
- 코드와 설명을 함께 정리하세요
- 정기적으로 복습하고 업데이트하세요
- 실전에서 활용할 수 있도록 실용적으로 정리하세요 